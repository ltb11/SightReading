package com.filedialog;

import android.widget.GridLayout;
import android.widget.Checkable;
import android.widget.CheckBox;

import android.content.Context;
import android.util.AttributeSet;



public class FileItem  {
   /* private static float CHECKMARK_AREA=-1;
    
    private CheckBox mCheckBox;
    private long mFileId;
    private String mFileName;
    private String mMimeType;
    private int mPosition;
    
    private int mPosition;

    public FileItem(Context c, AttributeSet as, int dS){
        super(c,as,dS);
        init();
    }

    public FileItem(Context c, AttributeSet as){
        super(c,as);
        init();
    }
    public FileItem(Context c){
        super(c);
        init();
    }
    
    private void init(){
        if(CHECKMARK_AREA == -1) CHECKMARK_AREA = getResources().getDimensionPixelSize(R.dimen.checkmark_area);
    }

    @Override
    public void onFinishInflate(){
        super.onFinishInflate();
        mCheckBox = (CheckBox) findViewById(R.id.file_checkbox);
    }
    
    public void setData(long fileId, int position, String fileName, String mimeType){
        mFileId = fileId;
        mPosition = position;
        mFileName = fileName;
        mMimeType = mimeType;
    }

    @Override
    public void setChecked(boolean b) {

    }

    @Override
    public boolean isChecked(){
        return mCheckBox.isChecked();
    }

    @Override
    public void toggle() {

    }

*/
}
